- move sensor to another group (?)
- sensors device ids shifted after each boot (i.e. hwmon4 becames hwmon5 and so on)
- add hostname to index page title
- min/max values autoscaling
	partialy done: server and sensor editor sides
	have no idea how to update sensor min/max data-set if autoscaling occures (on server side)
- rework editors (group and sensor)
- probe.sh is not finished
- Makefile: system install
- option for group for responsive sensors (scale them on group resizing)
- TODOs in internal/*
- update README.md
- lock layout (top transparent div to prevent clicks?)
- battery widget responsivness is very bad
- sensor setup error messages -> display them
- analog gauge widget
- force-run/redraw widget (menu option?)
- save/load dedicated backup (layout and sensors)
- per-client config (cookie-based? login? other?)
- dedicated 'system state' websocket packets: hostname, top10 processes, last10 dmesg lines, uptime, etc (non-numeric values)
- BUG: widget hint not shown
- BUG: dragged sensor moves over group boundaries
- separate options for widgets (popup "setup widget" in editor?)
- unify editor: build it from template for group/sensor/widget?
- grid (for graph widget) should show real min/max labels instead of 0,50,100%. Make it dynamic?
- keep diff history in ws.js along with values history
- rotated battery widget is not responsive (use SVG to draw it)

+ security: check for '..' in cmd paths
+ deal with long polling sensors: no init data upon client connection
+ smaller widget value suffix text
+ "not connected" message
+ sensor history? good for graphs building, disable if not graph sensor?
+ graph sensors
+ sensor container backgroud color and alpha
+ responsive sensor fonts
+ use ABS value or DIFF in widgets - make it selectable
+ BUG: menus do not respect screen edges (must stay within viewport)

